
import 'package:flutter/material.dart';

/// Holds all static variables for Text styles here.
/// This prevents us from re-writing the same code
/// over and over to set the colors of Text fields.
class ThemeTextStyle{
  static TextStyle loginTextFieldStyle = TextStyle(color: Colors.blueGrey);

}